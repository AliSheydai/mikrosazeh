---
title: نصب و راه اندازی
description: شروع به کار با میکروسازه
icon: ListStart
---

### 1. نصب ابزارهای اولیه

تمام کامپوننت‌ها از [Tailwind CSS v4](https://tailwindcss.com/docs/installation/framework-guides/nextjs) استفاده می‌کنند، پس مطمئن شوید که Tailwind در پروژه شما نصب شده باشد. بسیاری از کامپوننت‌ها همچنین از تابع کمکی `cn` استفاده می‌کنند. برای نصب آن، از دستور زیر استفاده کنید:

<Tabs items={["bun", "npx", "pnpm"]}>

```bash tab="bun"
bunx shadcn@latest add https://ui.mikrosazeh.ir/r/utils.json
```

```bash tab="npx"
npx shadcn@latest add https://ui.mikrosazeh.ir/r/utils.json
```

```bash tab="pnpm"
pnpm dlx shadcn@latest add https://mikrosazeh.ir/r/utils.json
```

</Tabs>

### 2. تمام شد 🎉

ما برای بیشتر کامپوننت‌هایی که آیکون دارند از [lucide-icons](https://lucide.dev/guide/installation) و همچنین برخی کامپوننت‌های [shadcn/ui](https://ui.shadcn.com/) استفاده می‌کنیم. این وابستگی‌ها هنگام استفاده از CLI به‌صورت خودکار نصب می‌شوند.

برای مثال، اگر بخواهید کامپوننت `card-05` را به پروژه خود اضافه کنید، کافی است دستور زیر را اجرا کنید:

<Tabs items={["bun", "npx", "pnpm"]}>
 
```bash tab="bun"
bunx shadcn@latest add https://ui.mikrosazeh.ir/r/card-05.json
```
 
```bash tab="npx"
npx shadcn@latest add https://ui.mikrosazeh.ir/r/card-05.json
```

```bash tab="pnpm"
pnpm dlx shadcn@latest add https://ui.mikrosazeh.ir/r/card-05.json
```

</Tabs>

اگرچه امکان کپی مستقیم کد وجود دارد، اما پیشنهاد می‌کنیم برای نصب کامپوننت‌ها حتماً از CLI استفاده کنید؛ چون این روش تضمین می‌کند تمام فایل‌های موردنیاز به‌درستی اضافه شوند.

حالا کامپوننت را به صفحه خود اضافه کنید و تمام:

```jsx
import Card05 from "@/components/mikrosazeh/card-05";

export default function Page() {
  return <Card05 />;
}
```

import { Tab, Tabs } from "fumadocs-ui/components/tabs";

### 3. بلوک ها

Blocks مجموعه‌ای از چند کامپوننت هستند که در کنار هم یک بخش کامل را می‌سازند و شامل چندین فایل مختلف می‌شوند. برای نصب Blocks، استفاده از CLI الزامی است. برای مثال، اگر بخواهید بلاک `ai-card-generation` را نصب کنید:

<Tabs items={["bun", "npx", "pnpm"]}>
 
```bash tab="bun"
bunx shadcn@latest add https://ui.mikrosazeh.ir/r/ai-card-generation.json
```
 
```bash tab="npx"
npx shadcn@latest add https://ui.mikrosazeh.ir/r/ai-card-generation.json
```

```bash tab="pnpm"
pnpm dlx shadcn@latest add https://ui.mikrosazeh.ir/r/ai-card-generation.json
```

</Tabs>

سپس آن را به صفحه خود اضافه کنید:

```jsx
/**
 * AICardGeneration require 5 additional components
 * that will be automatically installed with the CLI.
 */
import AICardGeneration from "@/components/mikrosazeh/AICardGeneration";

export default function Page() {
  return <AICardGeneration />;
}
```

### 4. وابستگی‌های اختیاری

برخی کامپوننت‌ها به کتابخانه‌های اضافی نیاز دارند که در انتهای صفحه هر کامپوننت لیست شده‌اند. برای اطمینان از عملکرد صحیح کامپوننت، حتماً این وابستگی‌ها را نصب کنید.

### 5. پروژه‌های چندریپویی

در پروژه‌های Monorepo، CLI مربوط به `shadcn/ui` از گزینه `-c` برای مشخص‌کردن مسیر workspace پشتیبانی می‌کند. برای مثال:

<Tabs items={["bun", "npx", "pnpm"]}>
 
```bash tab="bun"
bunx shadcn@latest add https://ui.mikrosazeh.ir/r/card-05.json -c ./apps/www
```
 
```bash tab="npx"
npx shadcn@latest add https://ui.mikrosazeh.ir/r/card-05.json -c ./apps/www
```

```bash tab="pnpm"
pnpm dlx shadcn@latest add https://ui.mikrosazeh.ir/r/card-05.json -c ./apps/www
```

</Tabs>
